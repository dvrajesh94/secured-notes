/**
 * This file contains all the action type constants, all the constants to be
 * exported. use UPPER_CASE_SNAKE_CASE for constant name. below is example for reference.
 */
const navigationPrefix = 'navigation';

const settingsPrefix = 'settings';

export const NAVIGATION_CONTENT_SELECTED = `${navigationPrefix}/CONTENT_SELECTED`;

export const THEME_SELECTED = `${settingsPrefix}/THEME_SELECTED`;
